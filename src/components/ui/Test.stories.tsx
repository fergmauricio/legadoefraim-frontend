import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta = {
  title: "Debug/Test",
  render: () => (
    <button className="bg-blue-500 text-white p-4">Test Button</button>
  ),
};
export default meta;

export const Primary: StoryObj = {};
